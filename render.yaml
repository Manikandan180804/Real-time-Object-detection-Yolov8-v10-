services:
  - type: web
    name: real-time-object-detection
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --workers 1 --threads 2 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0 # Stable version for deployment
